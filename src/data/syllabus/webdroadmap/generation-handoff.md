# Generation handoff: Web Engineering

- Authored batch: Day 1, `HTML Foundations — Your First Real Web Page`, in `public/data/lectures/webd/phase1/day01-a-real-page-on-the-internet.md`; Day 2, `HTML Forms, Inputs, Tables, and Element Flow`, in `public/data/lectures/webd/phase1/day02-make-the-page-readable.md`; Day 3, `CSS Foundations — Selectors, Color, Text, Units`, in `public/data/lectures/webd/phase1/day03-layouts-that-survive-real-content.md`.
- Actually explained: Day 1 now derives the source-to-DOM path, local editing loop, document metadata, tag and attribute grammar, browser repair of invalid nesting, heading depth, text semantics, nested lists, link destinations, contextual alternative text, image loading decisions, landmark selection, DevTools tree inspection, Git snapshots, and GitHub Pages diagnosis. Day 2 now traces successful controls into `name=value` pairs, label relationships, GET and POST transport, text and specialized input contracts, autocomplete, identifiers versus quantities, time-zone limits, constraint validation, readonly and disabled states, checkbox and radio payloads, select defaults, textareas, button intent, multipart upload, table header associations, disclosure, and normal flow. Day 3 now traces stylesheet requests, CSS parsing, selector matching, classes and attributes, combinators and pseudo-classes, per-property cascade resolution, specificity, inheritance, color models, alpha, backgrounds, font fallback and loading, line-height computation, text wrapping and truncation, relative units, custom properties, cursor honesty, and ordered DevTools diagnosis.
- Prerequisite repairs: This remains the launch batch, so Day 1 teaches the local server address, DOM, character decoding, viewport metadata, and Git stage boundaries before use. Day 2 retrieves Day 1 labels, IDs, paths, and semantics, then teaches every form and table mechanism locally. Day 3 retrieves the Day 1 path model and Day 2 form structure; it introduces the Network panel before relying on request status and derives all CSS vocabulary from visible failures. No box-model mechanics, Flexbox, or Grid were moved forward.
- Next syllabus unit: Day 4, `The Box Model — Spacing, Borders, Shadows`. Do not assume content-box versus border-box sizing, padding and margin mechanics, margin collapse, outlines versus borders, overflow policy, shadows, or divider technique as learned knowledge.
- Retrieval candidates: reconstruct the minimum document and diagnose a repaired DOM tree from Day 1; predict every included and omitted form pair, then change a one-choice requirement into an independent-choice requirement from Day 2; classify a CSS failure as load, match, cascade, or valid-but-ineffective, then resolve a changed specificity conflict from Day 3. Suitable changed domains are a lost-pet notice, library reservation, and quiet service advisory.
- Verification: directly read the intact mature WebD Day 13, Day 14, Day 15, and Day 37 references from `/Users/luckysolanki/Desktop/vault/snshnpngn/`; preserved all existing quiz payloads byte-for-byte; `npm run validate:quizzes` passed with 98 valid blocks across 18 lectures; scoped `git diff --check` passed for the four authorized files. The production build and rendered lecture, phase, metadata, sitemap, favicon, and OG routes passed final server checks.
- Learner evidence: unknown unless the learner supplies actual attempts or results.
