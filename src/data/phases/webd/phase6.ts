import { Phase } from '@/types';

export const phase6: Phase = {
  id: 'phase6',
  number: 6,
  title: 'Databases — MongoDB + Postgres',
  subtitle: 'Two real data layers: document (Mongoose) and relational (Drizzle + Postgres)',
  duration: '16 Days | ~68 Hours',
  days: 'Days 64-79',
  goal: 'Model and query real data in MongoDB, then put Postgres behind your Node API with Drizzle — schema as TypeScript, migrations, relations, transactions — and know exactly when to reach for each. You already know SQL; this teaches Drizzle from zero and how to spend that SQL knowledge inside a real backend.',
  icon: '🗄️',
  color: 'emerald',
  sections: [
    {
      id: 'day64',
      title: 'Day 64: Mongo Concepts & Atlas',
      duration: '4 hours',
      topics: [
        {
          id: 'doc-vs-rel',
          title: 'Document vs relational',
          duration: '40 mins',
          items: [
            'Documents = nested JSON-ish records',
            'No joins by default; aggregation does heavier lifting',
            'When Mongo wins: hierarchical/embedded data, schema-flexible early projects',
            'When SQL/Postgres wins: heavy joins, strict transactions, reporting (you\'ll wire Postgres on Day 75)',
          ],
        },
        {
          id: 'atlas',
          title: 'Atlas free cluster',
          duration: '50 mins',
          items: [
            'Create org, project, M0 free cluster',
            'Network access (your IP for dev; 0.0.0.0/0 for hosted later)',
            'DB user with strong password',
            'Connection string: mongodb+srv://...',
          ],
        },
        {
          id: 'mongosh',
          title: 'mongosh basics',
          duration: '60 mins',
          items: [
            'Connect with mongosh "<connection>"',
            'use <db>; db.coll.insertOne / find / updateOne / deleteOne',
            'Filter syntax: { field: { $gt: 5 } }',
            'BSON types & ObjectIds',
          ],
        },
        {
          id: 'compass',
          title: 'Compass GUI tour',
          duration: '30 mins',
          items: [
            'Install MongoDB Compass',
            'Browse collections visually',
            'Build queries via UI then export to code',
          ],
        },
      ],
    },
    {
      id: 'day65',
      title: 'Day 65: Mongoose Schemas & Models',
      duration: '4 hours',
      topics: [
        {
          id: 'why-mongoose',
          title: 'Why Mongoose',
          duration: '30 mins',
          items: [
            'Schema validation + types in app code',
            'Hooks (pre-save, post-find)',
            'Query builder fluent API',
            'Population (cheap joins)',
          ],
        },
        {
          id: 'schema',
          title: 'Schema design',
          duration: '60 mins',
          items: [
            'new mongoose.Schema({ name: { type: String, required: true } })',
            'enum, default, validators',
            'Subdocuments and arrays of subdocuments',
            'timestamps: true → createdAt/updatedAt free',
          ],
        },
        {
          id: 'model',
          title: 'Models & connections',
          duration: '50 mins',
          items: [
            'mongoose.model("User", userSchema)',
            'Singleton connection at app boot',
            'Use connection events (error/disconnect) for logging',
          ],
        },
        {
          id: 'indexes-schema',
          title: 'Schema-level indexes + typing',
          duration: '40 mins',
          items: [
            'unique: true on email',
            'index: true on lookup fields',
            'Compound + text indexes (Days 68-70 go deep on all of this)',
            'Typing models with TypeScript (InferSchemaType / interfaces)',
          ],
        },
      ],
    },
    {
      id: 'day66',
      title: 'Day 66: CRUD with Mongoose',
      duration: '4 hours',
      topics: [
        {
          id: 'create-read',
          title: 'Create / read',
          duration: '50 mins',
          items: [
            'Model.create(doc) → returns saved doc',
            'Model.find(filter) — returns array',
            'Model.findOne(filter), findById(id)',
            'Projections: .select("name email -_id")',
          ],
        },
        {
          id: 'update-delete',
          title: 'Update / delete',
          duration: '50 mins',
          items: [
            'updateOne, updateMany, findOneAndUpdate({ new: true })',
            'deleteOne, deleteMany, findOneAndDelete',
            'Atomic operators: $set, $inc, $push, $pull',
          ],
        },
        {
          id: 'lean',
          title: '.lean() and when to use it',
          duration: '30 mins',
          items: [
            '.lean() returns plain JS objects (no Mongoose docs)',
            'Much faster for read-heavy endpoints',
            'Lose schema methods + getters',
          ],
        },
        {
          id: 'operators',
          title: 'Common query operators',
          duration: '50 mins',
          items: [
            'Comparison: $gt, $gte, $lt, $lte, $ne, $in, $nin',
            'Logical: $and, $or, $not, $nor',
            'Element: $exists, $type',
            'String: $regex (with caution)',
          ],
        },
      ],
    },
    {
      id: 'day67',
      title: 'Day 67: Relationships & Population',
      duration: '4 hours',
      topics: [
        {
          id: 'embed-vs-ref',
          title: 'Embed vs reference',
          duration: '50 mins',
          items: [
            'Embed when: small, owned, accessed together',
            'Reference when: shared, large, accessed independently',
            'The 16MB document limit',
          ],
        },
        {
          id: 'populate',
          title: 'populate()',
          duration: '60 mins',
          items: [
            'ref: "User" in schema',
            'Query.populate("author") joins on the fly',
            'Nested populate, multiple paths',
            'Select fields when populating',
          ],
        },
        {
          id: 'n-plus-1',
          title: 'N+1 problem',
          duration: '40 mins',
          items: [
            'Looping queries instead of one populate',
            'Use $in or populate to fetch in bulk',
            'Aggregation $lookup as the heavy alternative',
            'The same N+1 trap exists in every data layer — you\'ll meet it again in Drizzle on Day 77',
          ],
        },
        {
          id: 'denormalize',
          title: 'Denormalize on purpose',
          duration: '30 mins',
          items: [
            'Cache author name on a post for feed perf',
            'Updates need fan-out — accept the cost',
            'Common pattern in social/feed apps',
          ],
        },
      ],
    },
    {
      id: 'day68',
      title: 'Day 68: Indexes from Scratch — How a Database Finds a Row',
      duration: '4 hours',
      topics: [
        {
          id: 'no-index-baseline',
          title: 'The problem: what happens when there is no index',
          duration: '40 mins',
          items: [
            'A table or collection is a heap — rows are stored in whatever order they were written, grouped into fixed-size pages (Postgres uses 8 KB pages; WiredTiger uses blocks). Nothing is sorted by the column you are searching',
            'So finding one row means reading every page: a sequential scan in Postgres, a COLLSCAN in Mongo. The cost is linear in pages, and it is dominated by I/O, not by comparing values',
            'The unit that matters is the page read, not the row. Ten rows in one page cost one read; ten rows spread across ten pages cost ten',
            'Why you have never felt this: at a thousand rows the whole table sits in memory and every query is instant. The behaviours only separate once the data outgrows the cache',
            'The mental image to keep: a scan is reading every page of a book to find one word; the index at the back is a completely separate, sorted structure that tells you the page number',
            'That is the whole definition — an index is a second, ordered data structure that maps a key to a row location, kept in sync on every single write',
          ],
        },
        {
          id: 'btree-internals',
          title: 'The B-tree, built up from first principles',
          duration: '45 mins',
          items: [
            'Start with a sorted array: binary search finds a key in O(log n), but inserting in the middle shifts everything — unusable for a database that writes',
            'Now branch it: a balanced tree whose nodes are pages, each holding hundreds of keys. Height stays tiny because each level multiplies the reach by the fanout',
            'B+tree, which is what both engines actually use: internal nodes hold only separator keys to route the search, all real entries live in the leaves, and the leaves are linked in order',
            'That leaf link is why range scans are cheap — find the start once, then walk sideways instead of re-descending the tree for every row',
            'Put numbers on it: with a fanout of a few hundred, three or four levels covers hundreds of millions of rows. Any lookup is three or four page reads, and the top levels are always cached',
            'Inserts split a full page and deletes merge sparse ones, which keeps the tree balanced. That rebalancing is exactly the write cost you are trading read speed for',
            'Postgres B-tree indexes and MongoDB/WiredTiger indexes are the same shape with different bookkeeping — learn it once, answer for both',
          ],
        },
        {
          id: 'what-index-stores',
          title: 'What is actually inside an index entry',
          duration: '40 mins',
          items: [
            'An entry is a pair: the key value, and a pointer to the row. In Postgres that pointer is a ctid (page number + offset); in Mongo it is the RecordId inside the collection file',
            'So a normal indexed read is two steps: descend the index to get pointers, then fetch the rows from the heap. Every plan you read will show these as separate stages (Index Scan then a heap fetch; IXSCAN then FETCH)',
            'When the index already contains every column the query needs, step two disappears — an index-only scan in Postgres, a covered query in Mongo. This is the single biggest win available from index design',
            'Clustered vs secondary, said precisely: Postgres has no clustered index (the heap is unordered, and CLUSTER is a one-time physical reorder that does not stay true). MySQL InnoDB stores the whole row in the primary key\'s B-tree. Mongo\'s _id index is a secondary index like any other',
            'Order inside the index is defined by the type and collation of the key, which is why a text index depends on collation, and why comparing a string to a number cannot use the index at all',
            'An index physically duplicates its key columns. That storage is real and shows up in your backups, your memory budget, and your cache hit rate',
          ],
        },
        {
          id: 'selectivity',
          title: 'Selectivity, cardinality, and why the planner ignores your index',
          duration: '40 mins',
          items: [
            'Cardinality is how many distinct values a column has. Selectivity is the fraction of rows a predicate keeps — the number that actually decides whether an index helps',
            'An index on a two-value status column is usually useless on its own: reading 40% of a table through an index is slower than just scanning the table',
            'The threshold both engines behave around: past roughly 5-10% of the table, a sequential scan wins. That is not a bug or a tuning failure, it is arithmetic',
            'The reason is random versus sequential I/O — an index scan jumps to a different heap page per row, while a scan streams pages in physical order and the OS reads ahead',
            '"I added an index and nothing changed" is the most common index complaint, and the answer is almost always that the planner measured and chose correctly',
            'Estimates come from statistics: Postgres builds histograms and n_distinct during ANALYZE; Mongo samples and caches a winning plan. Stale statistics after a bulk load are a genuine cause of terrible plans',
            'The interview version of this idea: an index is only useful when it eliminates most of the table',
          ],
        },
        {
          id: 'write-cost',
          title: 'What an index costs you',
          duration: '40 mins',
          items: [
            'Every insert writes the row plus one entry in every index on that table. Eight indexes means nine writes for one logical insert',
            'Updates are worse than they look: changing an indexed column is a delete plus an insert inside that index. Postgres HOT updates skip index churn only when no indexed column changed and the page has free space',
            'Deletes leave dead entries behind; the space is reclaimed later by autovacuum in Postgres. That lag is where index bloat comes from',
            'Indexes compete with data for memory — shared_buffers and the WiredTiger cache hold both, so an index nobody uses evicts pages somebody does',
            'This is why "index every column" is a production incident rather than a style disagreement: write throughput collapses and the cache stops helping',
            'The honest one-sentence framing to carry into interviews: an index is a bet that reads of this exact shape are frequent enough to justify a permanent tax on every write',
          ],
        },
        {
          id: 'first-measurements',
          title: 'Measure it yourself, in both engines',
          duration: '35 mins',
          items: [
            'Seed a table and a collection with a few hundred thousand rows — small data teaches nothing here because everything is already in memory',
            'Postgres: run EXPLAIN ANALYZE on the same query before and after CREATE INDEX, and watch Seq Scan become Index Scan alongside the actual time',
            'Mongo: run .explain("executionStats") before and after createIndex, and watch COLLSCAN become IXSCAN',
            'The one ratio to internalise: rows or documents examined versus returned. 1:1 is perfect, 1000:1 means the engine is doing work your index should have eliminated',
            'Now do it deliberately wrong: index a low-selectivity column, watch the planner refuse to use it, and confirm you can explain why',
            'Record the numbers. This harness is what you will use for the next two days and every slow query for the rest of your career',
          ],
          project: {
            title: 'Municipal Permit Index Benchmark',
            description: 'Generate 300k synthetic permit applications with district, category, submittedAt, riskScore, and status; seed identical records into Postgres and Mongo. Measure equality, range, and sorted queries before/after indexing, report rows examined versus returned and wall time, and explain one case where ignoring the index is correct.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day69',
      title: 'Day 69: Index Types & Index Design — Postgres and MongoDB',
      duration: '4 hours',
      topics: [
        {
          id: 'composite-order',
          title: 'Composite indexes and why column order decides everything',
          duration: '45 mins',
          items: [
            'One index on (a, b) is a completely different object from two indexes on a and on b — the composite sorts by a, then by b within each a',
            'The left-prefix rule follows directly from that sort order: an index on (a, b, c) serves queries on a, on (a, b), and on (a, b, c) — and never on b alone',
            'Mongo states the design rule as ESR: Equality fields first, then the Sort field, then Range fields. It is the most-quoted Mongo index rule and it is just a description of how a B-tree is walked',
            'The identical rule in Postgres vocabulary: equality columns first, then the column you ORDER BY, then the column with the range predicate',
            'Why a range before the sort column breaks everything: once you scan a range, the remaining columns are no longer globally ordered, so the engine must materialise and sort the result',
            'Index intersection (combining two single-column indexes) exists in both engines but is usually beaten by one correct composite. Know that it exists; do not design around it',
            'The design method: write the query first, list its equality predicates, its sort, and its ranges — the index falls out in that order',
          ],
        },
        {
          id: 'covering',
          title: 'Covering indexes and index-only scans',
          duration: '40 mins',
          items: [
            'If every column a query touches lives in the index, the heap fetch disappears entirely and the query is answered from the index alone',
            'Postgres calls this an Index Only Scan. INCLUDE columns let you carry extra payload in the leaves without affecting sort order or uniqueness',
            'Postgres still consults the visibility map, so a table with many recent updates can fall back to heap fetches until autovacuum catches up — the classic "why is my index-only scan not index-only" question',
            'Mongo calls it a covered query: the projection must exclude _id unless _id is in the index, and explain shows totalDocsExamined: 0 when you got it right',
            'The trade-off is honest: a wider index is a bigger index, and every extra column is paid on every write and in cache',
            'Where it wins hardest: the high-volume list endpoint that returns three fields out of thirty',
          ],
        },
        {
          id: 'pg-index-types',
          title: 'Postgres index types beyond the default B-tree',
          duration: '45 mins',
          items: [
            'B-tree is the default and the right answer for equality, ranges, sorting, and uniqueness — reach for something else only when the data shape demands it',
            'Hash: equality only, no ranges and no ordering. Rarely worth it because B-tree already handles equality well',
            'GIN: for values that contain many keys — jsonb containment, arrays, and full-text search over tsvector. Slower to write, extremely fast to search',
            'GiST: geometric and nearest-neighbour searches, range types, and exclusion constraints (no two bookings overlapping the same room)',
            'BRIN: a tiny index that stores min/max per block range. Brilliant on huge naturally-ordered tables like append-only event logs, useless on randomly ordered data',
            'SP-GiST for space-partitioned structures — knowing the name and the use case is enough',
            'Expression indexes: CREATE INDEX ON users (lower(email)) is the fix for "my WHERE clause calls a function and the index stopped working"',
            'Partial indexes: CREATE INDEX ... WHERE status = \'active\' builds an index over only the hot subset — smaller, faster, cheaper to maintain',
            'Unique indexes are the mechanism behind uniqueness constraints, which is why a duplicate insert surfaces as an index error',
          ],
        },
        {
          id: 'mongo-index-types',
          title: 'MongoDB index types',
          duration: '45 mins',
          items: [
            'Single-field and compound indexes, plus the _id index every collection gets whether you asked or not',
            'Multikey: an index on an array field creates an entry per element, so a document matches if any element matches. One compound index cannot contain two array fields — the entry count would multiply',
            'Text indexes: one per collection, language-aware stemming, queried with $text. Fine for modest search; Atlas Search exists because real search needs more',
            'Wildcard indexes for genuinely unpredictable key names in flexible documents — an escape hatch, never a default',
            'Hashed indexes: equality only, and the basis for hash-based shard keys',
            '2dsphere for real-world geospatial queries',
            'Partial indexes (partialFilterExpression) versus the older sparse indexes — partial is more general and is what you should reach for',
            'Unique indexes, and the unique + partial combination that lets many documents omit the field while keeping the present values unique',
            'TTL indexes: expireAfterSeconds lets a background job delete expired documents — sessions, carts, one-time tokens, anything ephemeral',
            'Collation on an index for case-insensitive matching, so you stop writing regex for what is really an equality test',
          ],
        },
        {
          id: 'engine-mapping',
          title: 'The same problem in both engines, side by side',
          duration: '35 mins',
          items: [
            'Equality plus sort plus range: a composite B-tree in both, with the same column-order rule',
            '"Find everything tagged X": a GIN index on an array or jsonb column in Postgres, a multikey index in Mongo',
            'Full-text search: tsvector + GIN in Postgres, a text index or Atlas Search in Mongo',
            'Expire old records: a scheduled delete job in Postgres, a TTL index in Mongo',
            'Uniqueness with exceptions: a partial unique index in both, spelled differently',
            'Case-insensitive lookup: an expression index on lower(col) in Postgres, an index collation in Mongo',
            'The interview takeaway: the mechanisms are nearly identical across engines and only the vocabulary changes — say that out loud and you sound like someone who has used both',
          ],
        },
        {
          id: 'design-workshop',
          title: 'Designing an index set for a real workload',
          duration: '30 mins',
          items: [
            'Start from the queries you actually run, not from the entity diagram. Indexes serve query shapes, not tables',
            'One well-ordered composite usually beats three single-column indexes aimed at the same query',
            'Hunt redundancy: an index on (a) is dead weight when (a, b) already exists, because the prefix rule already covers it',
            'Budget deliberately — every index costs write throughput, storage, and cache. Past a handful on one table, each new one needs a written justification',
            'Name indexes after the query they serve and review them in pull requests like any other code',
          ],
          project: {
            title: 'Festival Schedule Index Design',
            description: 'List six real festival-schedule queries: events by day, venue timetable, accessibility filter, performer lookup, text search, and upcoming changes. Break down equality/sort/range needs, design the smallest supporting index set, justify every index, and record one deliberately omitted index.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day70',
      title: 'Day 70: Query Plans, Diagnosis & Index Maintenance',
      duration: '4 hours',
      topics: [
        {
          id: 'pg-explain',
          title: 'Reading a Postgres query plan',
          duration: '45 mins',
          items: [
            'EXPLAIN shows the plan and the estimates; EXPLAIN (ANALYZE, BUFFERS) actually runs it and shows reality. Use the second one — estimates alone have misled everyone at least once',
            'Read a plan bottom-up and inside-out: the deepest nodes run first and feed their parents',
            'The node vocabulary you must recognise on sight: Seq Scan, Index Scan, Index Only Scan, Bitmap Index Scan feeding a Bitmap Heap Scan, Nested Loop, Hash Join, Merge Join, Sort, Aggregate',
            'Bitmap scans, explained properly: when a predicate matches too many rows for repeated random fetches but too few to justify scanning everything, Postgres collects matching page addresses first, then reads those pages in physical order',
            'The first thing to check on any slow query: estimated rows versus actual rows. A 100x misestimate is a statistics problem, and adding indexes will not fix it',
            'BUFFERS tells you what came from cache and what came from disk — the difference between a genuinely slow query and a cold cache',
            'A Sort node reporting "external merge Disk" means it spilled; the fix is either work_mem or an index that returns rows already ordered',
          ],
        },
        {
          id: 'mongo-explain',
          title: 'Reading a MongoDB query plan',
          duration: '40 mins',
          items: [
            'explain() has three verbosities: queryPlanner (what it would do), executionStats (what it did), allPlansExecution (what it tried before choosing)',
            'The stages to know: COLLSCAN, IXSCAN, FETCH, SORT, SORT_MERGE, PROJECTION_COVERED, LIMIT',
            'winningPlan versus rejectedPlans: Mongo races candidate plans on a sample, picks a winner, and caches it. A cached plan can outlive the data distribution that justified it',
            'The three numbers that diagnose almost everything: nReturned, totalKeysExamined, totalDocsExamined. Keys examined far above returned means the wrong column order; docs examined above zero on a projection-only query means it is not covered',
            'A SORT stage means the sort happened in memory rather than coming free from the index — and in-memory sorts hit a hard 32 MB limit that turns into a runtime error exactly when your data grows',
            'hint() forces a specific index, which is a diagnostic tool for comparing plans and almost never something to ship',
            'The database profiler and slow-query log are where real work starts: you fix the queries that are actually slow in production, not the ones that feel slow',
          ],
        },
        {
          id: 'antipatterns',
          title: 'Queries that silently refuse to use your index',
          duration: '40 mins',
          items: [
            'A function wrapped around the column (lower(email), date_trunc, $expr) — the index stores the raw value, so the engine cannot match. Fix with an expression index or by moving the function to the other side',
            'Type mismatch or an implicit cast, the classic being a numeric id compared against a string. The index is on one type and your predicate is on another',
            'Leading-wildcard matching, LIKE \'%term\' or an unanchored $regex — there is no prefix to seek to, so the whole index is walked at best',
            'Negations: !=, NOT IN, $ne, $nin all mean "almost everything matches", which is exactly when a scan is cheaper',
            'OR across different columns often defeats a single index; Postgres may use a bitmap union, and sometimes rewriting as UNION is genuinely faster',
            'ORDER BY on a column that is not next in the index order — you get the rows, then pay for a sort',
            'Large OFFSET or skip: the engine still walks and discards every skipped row. Keyset pagination is the fix, and it is the same fix in both engines',
            'Low selectivity: the index exists, the planner read the statistics, and a scan really is faster. Recognising this case is what separates guessing from diagnosis',
          ],
        },
        {
          id: 'maintenance',
          title: 'Statistics, bloat, and keeping an index set honest',
          duration: '40 mins',
          items: [
            'Postgres: ANALYZE refreshes the histograms the planner reasons with. After any bulk load, run it before you benchmark anything or you will draw the wrong conclusion',
            'Autovacuum reclaims dead tuples; when it falls behind, indexes bloat and index-only scans stop being index-only',
            'REINDEX CONCURRENTLY rebuilds a bloated index without blocking writes; CREATE INDEX CONCURRENTLY adds one to a live table the same way, more slowly, and can leave an invalid index behind if it fails',
            'Mongo: index builds on a live replica set, the memory they consume while building, and why you build them on secondaries first in serious deployments',
            'Finding dead weight: pg_stat_user_indexes with idx_scan = 0, and $indexStats in Mongo. An index nobody reads still costs every write, forever',
            'Watch size against memory: pg_relation_size and db.collection.stats().indexSizes. An index larger than available cache behaves nothing like one that fits',
            'Make redundant and duplicate index detection a routine review, not a heroic cleanup',
          ],
        },
        {
          id: 'advanced',
          title: 'The advanced answers interviewers dig for',
          duration: '40 mins',
          items: [
            'MVCC and indexes: Postgres index entries point at row versions, which is why index-only scans consult the visibility map and why an update to a non-indexed column can avoid touching indexes at all (HOT)',
            'Page splits on monotonically increasing keys create a right-edge hotspot; fill factor exists to leave room for updates. Random UUID v4 primary keys fragment the index badly, while time-ordered UUID v7 or ULID keys do not',
            'B-tree versus LSM-tree in one paragraph: B-trees update in place and favour reads; LSM-trees append and compact, favouring writes. This is why different engines pick different structures for different workloads',
            'Why the planner rarely picks index intersection, and why one composite index is almost always the better answer',
            'Indexes and sharding: in Mongo the shard key must be indexed, and whether a query includes it decides between a targeted query and a scatter-gather across every shard',
            'When the answer is not another index: partitioning a huge table, denormalising a hot read, or caching the result — an index cannot fix a query that legitimately touches most of the data',
            'Cost-model literacy: random_page_cost and effective_cache_size shape every Postgres plan decision, and on SSD-backed servers the defaults are often wrong',
            'Locking during DDL: know which index operations take which locks, because that is what turns a routine migration into an outage',
          ],
        },
        {
          id: 'interview-drill',
          title: 'Interview drill + fix your own slow queries',
          duration: '35 mins',
          items: [
            'Answer these out loud from memory, no notes: what is an index and what does it physically contain; how does a B-tree find a row and why is its height so small; why did my index not get used; what does the order of a composite index mean; what is a covering index; what does an index cost; how do you find a missing index; how do you find a useless one',
            'Harder follow-ups to rehearse: why is a scan sometimes faster; what is a bitmap heap scan; what breaks an index-only scan; what does ESR mean and why; why does a large OFFSET get slower',
            'Take three genuinely slow queries from your blog backend, predict each plan on paper first, then run explain and check whether you were right',
            'Fix one with a composite index, one with a covering index, and one by rewriting the query — three different tools, deliberately',
            'Write a one-page index rationale for your schema: every index, the query it serves, and its cost. This artifact is what turns a vague system-design answer into a credible one',
          ],
          project: {
            title: 'Clinic Waitlist Slow-Query Postmortem',
            description: 'Diagnose three slow clinic-waitlist reads: next patient per service, overdue triage cases, and clinician queue. Capture each plan, state a hypothesis, apply one fix, and compare examined/returned rows and wall time in Postgres and Mongo; leave one acceptable query unchanged and defend that choice.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day71',
      title: 'Day 71: Aggregation Pipeline',
      duration: '4 hours',
      topics: [
        {
          id: 'pipeline',
          title: 'Pipeline mental model',
          duration: '60 mins',
          items: [
            'Stages run in order; each transforms the stream',
            '$match early to use indexes',
            '$group, $project, $sort, $limit',
            'Each stage\'s output is next stage\'s input',
          ],
        },
        {
          id: 'lookup',
          title: '$lookup for joins',
          duration: '50 mins',
          items: [
            'Foreign-key style joins',
            'Returns array; flatten with $unwind',
            'Use sparingly (slower than populate for simple cases)',
          ],
        },
        {
          id: 'facet',
          title: '$facet for parallel pipelines',
          duration: '40 mins',
          items: [
            'Run multiple sub-pipelines on the same input',
            'Perfect for dashboards',
          ],
        },
        {
          id: 'practical',
          title: 'Practical patterns',
          duration: '30 mins',
          items: [
            'Top N by count',
            'Daily counts last 30 days',
            'Average rating per category',
          ],
        },
      ],
    },
    {
      id: 'day72',
      title: 'Day 72: Mongo Transactions & Data Integrity',
      duration: '4 hours',
      topics: [
        {
          id: 'atomicity',
          title: 'When you need atomicity',
          duration: '40 mins',
          items: [
            'Single-document writes are already atomic in Mongo',
            'Multi-document invariants need a transaction (transfer funds, decrement stock + create order)',
            'The exact interview question: "how do you make two writes all-or-nothing in Mongo?"',
          ],
        },
        {
          id: 'sessions',
          title: 'Sessions & multi-document transactions',
          duration: '60 mins',
          items: [
            'startSession() → session.withTransaction(async () => { ... })',
            'Pass { session } to every operation inside',
            'Commit / abort semantics; retry on transient errors',
            'Transactions require a replica set (Atlas gives you one)',
          ],
        },
        {
          id: 'integrity',
          title: 'Integrity without foreign keys',
          duration: '50 mins',
          items: [
            'Mongo has no FK constraints — the app enforces relationships',
            'Optimistic concurrency with a version field (__v / updatedAt guard)',
            'Idempotency keys so a retried request doesn\'t double-write',
            'Soft deletes vs hard deletes and their trade-offs',
          ],
        },
        {
          id: 'preview-sql-integrity',
          title: 'Preview: how SQL does this differently',
          duration: '30 mins',
          items: [
            'Relational DBs enforce FKs, uniqueness, and checks at the engine',
            'ACID transactions are first-class in Postgres',
            'This is a major reason to pick Postgres — you\'ll wire it next',
          ],
        },
      ],
    },
    {
      id: 'day73',
      title: 'Day 73: Data-model Trade-off Workshop',
      duration: '5 hours',
      topics: [
        {
          id: 'spec',
          title: 'Spec',
          duration: '20 mins',
          items: [
            'Models: User, Post, Comment, Tag',
            'Endpoints: posts CRUD, tag filter, comments threaded',
            'GET /stats: posts/day last 30 days (aggregation)',
            'Real Atlas DB, indexed for the search query',
          ],
        },
        {
          id: 'models',
          title: 'Models',
          duration: '90 mins',
          items: [
            'User schema (we wire real auth Phase 7)',
            'Post: title, slug (unique), body, tags ([String]), author (ref)',
            'Comment: post (ref), parent (ref or null), body, author (ref)',
            'Indexes: slug unique, text index on title+body, tags',
          ],
        },
        {
          id: 'endpoints',
          title: 'Endpoints',
          duration: '90 mins',
          items: [
            'CRUD posts with paginated GET',
            'Search via $text (use the text index)',
            'Filter by tag',
            'Threaded comments with population',
          ],
          project: {
            title: 'Field Survey Archive on Mongo (in progress)',
            description: 'Build a Mongo archive for field expeditions, sites, observations, specimens, and review notes with deliberate embedding/reference choices, supporting indexes, one aggregation, and one integrity-sensitive transaction. Finish and review it on Day 74.',
            type: 'project',
          },
        },
      ],
    },
    {
      id: 'day74',
      title: 'Day 74: Query, Index & Migration Review',
      duration: '5 hours',
      topics: [
        {
          id: 'stats',
          title: 'GET /stats aggregation + a transaction',
          duration: '75 mins',
          items: [
            '$match createdAt > 30 days ago, $group by day, $sort + $project',
            'Add one real transaction: create post + increment author.postCount atomically (Day 72)',
          ],
        },
        {
          id: 'wire-notes',
          title: 'Replace Notes API in-memory store with Mongo',
          duration: '105 mins',
          items: [
            'Add Note model',
            'Replace Map ops with Mongoose calls',
            'Keep all validation + middleware untouched',
            'Re-run your Vitest+Supertest suite against the Mongo-backed API',
          ],
        },
        {
          id: 'docs',
          title: 'Docs + capstone checkpoint',
          duration: '60 mins',
          items: [
            'README with data-model diagram (mermaid)',
            'Updated Postman collection',
            'Both Notes API + Blog backend on real Atlas',
          ],
          project: {
            title: 'Field Survey + Collection APIs (checkpoint)',
            description: 'Notes API + Blog backend, both on real Mongo Atlas, indexed, with an aggregation and a transaction. Next: give a relational database the same treatment.',
            type: 'project',
          },
        },
      ],
    },
    {
      id: 'day75',
      title: 'Day 75: Postgres Behind Your API — Drizzle from Zero',
      duration: '5 hours',
      topics: [
        {
          id: 'where-drizzle-sits',
          title: 'Driver, query builder, ORM — where Drizzle sits',
          duration: '45 mins',
          items: [
            'You already write SQL well. The only new problem is getting typed SQL into a Node process and typed rows back out',
            'Three layers to choose from: the raw driver (pg — you hand-write SQL strings and hand-type the results), a typed query builder (Drizzle), or a heavy ORM that owns a query language and a generated client',
            'What Drizzle is: a thin TypeScript layer that compiles to the SQL you would have written, plus drizzle-kit (a CLI for migrations) and Drizzle Studio (a data browser)',
            'What Drizzle is not: no generated client to regenerate after every schema edit, no query-engine binary, no new query language — db.select().from(users) is literally select ... from users',
            'Why that fits you: every SELECT/JOIN/GROUP BY instinct you already have transfers one-to-one, and .toSQL() prints the exact statement whenever you doubt what was built',
            'The honest trade-off: Drizzle will not hide a bad query from you. You own the shape of the SQL — which is an advantage only because you can read a query plan',
          ],
        },
        {
          id: 'provision-db',
          title: 'Provision Postgres + a connection string that works everywhere',
          duration: '50 mins',
          items: [
            'Managed Postgres with a free tier: Neon, Supabase, or Railway — create one database and keep the dashboard open',
            'Anatomy of the URL: postgresql://user:password@host:5432/dbname?sslmode=require — every part is something you can break in exactly one way',
            'DATABASE_URL lives in .env and is validated by your Zod env schema at boot (the Phase 5 pattern), so a missing or malformed URL fails at startup instead of on the first request',
            'Direct vs pooled connection strings: Postgres has a hard connection limit, and a process-per-request deploy will exhaust it — a pooler (PgBouncer, Neon\'s pooled host) is what you point at in production',
            'Run a local Postgres too (Docker or Postgres.app) so your dev loop is instant — same URL shape, different value, selected by environment',
            '.env stays out of git; .env.example documents which keys must exist',
          ],
        },
        {
          id: 'install-wire',
          title: 'Install Drizzle and create the db client',
          duration: '50 mins',
          items: [
            'npm i drizzle-orm pg and npm i -D drizzle-kit @types/pg — node-postgres is the default driver; postgres.js is the alternative and the Drizzle API above it is identical',
            'src/db/index.ts: import { drizzle } from "drizzle-orm/node-postgres" then export const db = drizzle(process.env.DATABASE_URL!)',
            'When you need pool options, build the pool yourself: new Pool({ connectionString, max: 10 }) → drizzle(pool)',
            'One db instance per process — import it everywhere, never construct one per request (that is the connection-storm bug)',
            'Version note so the docs never confuse you: npm i drizzle-orm installs the stable 0.4x line this phase teaches, while the official site currently documents the v1 release candidate (drizzle-orm@rc). Day 77 shows both relation APIs side by side',
            'Sanity check before writing any schema: await db.execute(sql`select 1`) — if that resolves, your URL, SSL, and network are all correct',
          ],
        },
        {
          id: 'first-table',
          title: 'Your first table — schema as TypeScript',
          duration: '55 mins',
          items: [
            'src/db/schema.ts: export const users = pgTable("users", { id: serial("id").primaryKey(), email: text("email").notNull().unique(), createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull() })',
            'That one object is both the runtime query builder and the type source — there is no second schema file to keep in sync with your code',
            'TypeScript name vs column name: varchar("first_name") gives you firstName in code and first_name in the database, so your app can read camelCase while the DB stays snake_case',
            'drizzle.config.ts: defineConfig({ schema: "./src/db/schema.ts", out: "./drizzle", dialect: "postgresql", dbCredentials: { url: process.env.DATABASE_URL! } })',
            'npx drizzle-kit push applies the schema straight to your dev database in one step — the versioned migration workflow arrives on Day 78, and push is a dev-only shortcut until then',
            'Verify in psql with \\d users: the table Drizzle created is exactly the table you would have written by hand',
          ],
        },
        {
          id: 'first-queries',
          title: 'Your first queries — and the SQL they compile to',
          duration: '55 mins',
          items: [
            'Insert: await db.insert(users).values({ email }).returning() — RETURNING is the Postgres feature you already use, exposed directly',
            'Select: await db.select().from(users) returns an array of typed rows keyed by your TypeScript property names',
            'Filter: import { eq } from "drizzle-orm" then .where(eq(users.id, id)) — operators are plain functions, which is why they compose instead of nesting in a DSL',
            '.toSQL() returns { sql, params }: read it whenever a result surprises you. Values are bound parameters, never concatenated into the string, so there is no injection surface',
            'A query builder is lazy: nothing runs until you await it (or call .execute()) — that is what lets you build a query in pieces across a function',
            'Wire it for real: GET /users and POST /users in your Phase 5 Express app, body validated by Zod, your existing error middleware untouched',
          ],
        },
        {
          id: 'studio-loop',
          title: 'Drizzle Studio + the day-to-day loop',
          duration: '45 mins',
          items: [
            'npx drizzle-kit studio opens a browser data browser generated from your schema — the Compass equivalent for this side of the course',
            'Inspect rows, edit one by hand, hit your endpoint, and confirm the change is visible: that round trip is how you debug data problems fast',
            'The rhythm for the rest of this phase: edit schema.ts → push → query → read the SQL when something surprises you',
            'The four failures you will actually hit first: DATABASE_URL unset, sslmode missing on a hosted database, a forgotten await (you get a builder object instead of rows), and importing the table from the wrong file after you split the schema',
          ],
          project: {
            title: 'Volunteer Registry Table, End to End',
            description: 'Add a real Postgres volunteer registry to the Phase 5 Express service with Drizzle: schema, configuration, migration, db singleton, and validated create/list endpoints. Print the generated SQL and explain the uniqueness, status, and availability constraints.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day76',
      title: 'Day 76: Modeling a Real Schema — Types, Constraints & Indexes',
      duration: '4 hours',
      topics: [
        {
          id: 'column-types',
          title: 'Column builders — the Postgres types you know, in TypeScript',
          duration: '40 mins',
          items: [
            'Identity/keys: serial("id") for the classic auto-increment, or integer("id").generatedAlwaysAsIdentity() for the modern SQL-standard form, or uuid("id").defaultRandom()',
            'Text: text() when length is unbounded, varchar("name", { length: 120 }) when the constraint is real',
            'Time: timestamp("created_at", { withTimezone: true }) — always timezone-aware for anything a user sees; the mode option decides whether JS gets a Date or a string',
            'Money: numeric("amount", { precision: 12, scale: 2 }) and never a float; it comes back as a string, so decide at the boundary where you convert',
            'JSON: jsonb("meta").$type<{ source: string }>() types the blob at compile time with zero runtime cost — the flexible corner you missed from Mongo, inside a relational table',
            'Booleans, date, integer, bigint (mode: "number" | "bigint") — pick the mode deliberately, because it decides what your API serializes',
          ],
        },
        {
          id: 'constraints',
          title: 'Constraints — pushing rules down into the engine',
          duration: '40 mins',
          items: [
            '.notNull() and .default(value) generate real DEFAULT clauses; .$defaultFn(() => nanoid()) runs in JS instead, for values the database cannot compute',
            '.unique() on a column, or uniqueIndex() in the table callback when you need a name or multiple columns',
            'Composite primary keys via primaryKey({ columns: [t.postId, t.tagId] }) in the table callback — the join-table pattern',
            'check("positive_qty", sql`quantity > 0`) puts an invariant where nothing can bypass it, including a future script or a teammate with psql open',
            'The rule of thumb: validate in Zod for a good error message, constrain in Postgres for the guarantee. Zod protects the user, the constraint protects the data',
          ],
        },
        {
          id: 'enums',
          title: 'Enums and controlled vocabularies',
          duration: '35 mins',
          items: [
            'pgEnum("role", ["user", "admin"]) creates a real Postgres enum type and gives you a union type in TypeScript for free',
            'Adding a value later is a migration, and removing one is worse — that cost is the reason to think before reaching for it',
            'The three options and when each wins: a Postgres enum for a short, stable set; a check constraint for a set you expect to edit; a lookup table when the values need their own columns (label, sort order, active flag)',
            'A status column is the classic case — model it once here and every state-machine question later gets easier',
          ],
        },
        {
          id: 'relations-fk',
          title: 'Foreign keys and the shapes of relationships',
          duration: '45 mins',
          items: [
            'authorId: integer("author_id").notNull().references(() => users.id, { onDelete: "cascade" }) — one line that is both a column and a constraint',
            'Referential actions are a product decision, not a default: cascade deletes the children, set null orphans them deliberately, restrict refuses the delete',
            'One-to-many is just the FK on the many side; one-to-one is the same plus a unique constraint',
            'Many-to-many needs an explicit join table (postsToTags) with a composite primary key — Drizzle does not invent hidden tables for you, which means the schema in the file is the schema in the database',
            'Self-references (a comment\'s parent) need a type annotation on the table const because TypeScript cannot infer a circular reference',
            'Declaring the FK does not give you nested reads — that is the relations() layer on Day 77. Keep the two ideas separate in your head',
          ],
        },
        {
          id: 'indexes',
          title: 'Indexes, declared next to the table',
          duration: '40 mins',
          items: [
            'The third argument to pgTable is a callback returning an array: (t) => [index("posts_author_idx").on(t.authorId), uniqueIndex("posts_slug_idx").on(t.slug)]',
            'Composite index column order follows the same left-prefix rule you learned on Day 69 — the index that serves (authorId, createdAt) does not serve createdAt alone',
            'Partial indexes: index("active_idx").on(t.status).where(sql`status = \'active\'`) — smaller index, cheaper writes, when most rows are irrelevant',
            'Index the columns your real query filters and sorts by, together, in that order — not every column that looks important',
            'Every index is paid for on write and in storage; the same trade-off as Mongo, verified the same way — read the plan, do not guess',
          ],
        },
        {
          id: 'inferred-types',
          title: 'Types out of the schema, validation out of the types',
          duration: '40 mins',
          items: [
            'type User = typeof users.$inferSelect gives you the row type; type NewUser = typeof users.$inferInsert makes defaults and generated columns optional — two lines that replace a hand-maintained interface',
            'drizzle-zod: createInsertSchema(users) derives a Zod schema from the table, so a column change breaks your validation at compile time instead of at runtime',
            'Refine what the database cannot express: createInsertSchema(users, { email: (s) => s.email(), bio: (s) => s.max(280) })',
            'createSelectSchema for validating what you send out, when a response contract matters',
            'Import note: this ships as the separate drizzle-zod package on the stable line, and moves to drizzle-orm/zod in v1 — check which one your package.json actually has',
            'Schema file layout: one file per domain area, re-exported from a barrel that drizzle.config.ts points at, so drizzle-kit sees every table',
          ],
          project: {
            title: 'Equipment Booking Schema in Drizzle',
            description: 'Model equipment, locations, borrowers, bookings, bookingItems, and maintenance holds relationally. Include an enum status, non-overlap/inventory constraints you can defend, explicit cascade rules, and indexes for availability and history. Derive insert types and Zod schemas from tables.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day77',
      title: 'Day 77: Querying with Drizzle — Builder & Relational Queries',
      duration: '4 hours',
      topics: [
        {
          id: 'select-filter',
          title: 'Select, filter, sort, paginate',
          duration: '45 mins',
          items: [
            'Full rows: db.select().from(posts). Partial select: db.select({ id: posts.id, title: posts.title }).from(posts) — and the return type narrows to exactly those keys',
            'Operators are imported functions: eq, ne, gt, gte, lt, lte, like, ilike, inArray, notInArray, isNull, isNotNull, between',
            'Compose them with and(), or(), not() — because they are values, you can build a filter array conditionally and spread it: and(...conditions)',
            'Sorting and slicing: .orderBy(desc(posts.createdAt)).limit(20).offset(40)',
            'Undefined conditions are skipped, which is what makes an optional query-param filter clean instead of a chain of if statements',
            'The whole point: this is your SQL, one layer thinner, with the column names checked by the compiler',
          ],
        },
        {
          id: 'joins',
          title: 'Joins and the result shape that surprises everyone',
          duration: '40 mins',
          items: [
            'db.select().from(posts).innerJoin(users, eq(posts.authorId, users.id)) — the join condition is the same predicate you would write in SQL',
            'The result is one object per row with a key per table: { posts: {...}, users: {...} } — not a merged flat object. Knowing this up front saves an hour of confusion',
            'leftJoin gives you null on the missing side, so the type of that side becomes nullable — the compiler forces you to handle the case you would have forgotten',
            'Reshape at the edge: map the rows into your API shape in one place, not in every route',
            'Multiple joins stack, and so does the row size — select the columns you need rather than dragging every column of three tables across the wire',
          ],
        },
        {
          id: 'aggregates',
          title: 'Aggregates, grouping, and the sql escape hatch',
          duration: '40 mins',
          items: [
            '.groupBy(posts.authorId) with count(), sum(), avg() imported from drizzle-orm; .having() filters the groups',
            'Anything Drizzle has no helper for: sql`coalesce(${posts.views}, 0)` — interpolated columns are rendered as identifiers and interpolated values as bound parameters, so it stays injection-safe',
            'Give computed columns a type: sql<number>`count(*)` so the result is not unknown',
            'db.execute(sql`...`) runs a statement Drizzle never sees the shape of — your full escape hatch for a window function or a recursive CTE you already know how to write',
            'Reach for raw SQL when the query is genuinely more expressive as SQL, not when you are stuck — and keep it in one repository file, not scattered through routes',
          ],
        },
        {
          id: 'relational-queries',
          title: 'Relational queries — nested reads without N+1',
          duration: '45 mins',
          items: [
            'Declare relations once: export const postsRelations = relations(posts, ({ one, many }) => ({ author: one(users, { fields: [posts.authorId], references: [users.id] }), comments: many(comments) }))',
            'Hand the schema to the client so db.query exists: drizzle(pool, { schema })',
            'Read a whole tree in one call: db.query.posts.findMany({ with: { author: true, comments: { limit: 3 } } }) — you get nested objects, not flat join rows',
            'Trim it: columns: { id: true, title: true } for partial selection, plus where, orderBy, limit and offset at every level',
            'findFirst for a single record; nesting goes as deep as your relations do',
            'This is the N+1 fix from Day 67 in relational form: one round trip instead of a query per parent row. relations() is only for this API — it never changes your tables or your migrations',
          ],
        },
        {
          id: 'builder-vs-query',
          title: 'Which API to use, and the version trap in the docs',
          duration: '35 mins',
          items: [
            'Use db.query when the response is a tree the client wants (a post with its author and comments)',
            'Use db.select() when you need aggregates, an unusual join, a hand-tuned plan, or a shape that is not a tree',
            'Both compile to SQL against the same connection and both are fully typed — this is a readability choice, not a performance cliff',
            'The trap: the official docs site now shows the v1 release candidate. There, relations() becomes defineRelations(), where becomes an object ({ id: 1 }), orderBy becomes { id: "asc" }, and many-to-many gets a through() helper',
            'Your installed stable version uses the callback form taught above. Check the version in package.json before you copy a snippet — this is the single most common reason a Drizzle example does not compile',
            'Nothing else you learned today changes between the two lines: tables, operators, joins, and transactions are the same',
          ],
        },
        {
          id: 'pagination-perf',
          title: 'Pagination and hot-path performance',
          duration: '35 mins',
          items: [
            'Offset pagination is fine until it is not: the database still walks the skipped rows, so page 500 is slow for the same reason a large Mongo skip was on Day 70',
            'Keyset (cursor) pagination: where(lt(posts.createdAt, cursor)).orderBy(desc(posts.createdAt)).limit(20) — stable under inserts, and it uses the index you already built',
            'Prepared statements for endpoints called constantly: const q = db.select().from(posts).where(eq(posts.id, sql.placeholder("id"))).prepare("post_by_id") then q.execute({ id })',
            'The parse-and-plan work happens once instead of per request — measurable on hot, simple queries, irrelevant on rare complex ones',
            'Take the string from .toSQL() into EXPLAIN ANALYZE and read the plan exactly as you already do. Drizzle changed how the query is written, not how Postgres executes it',
          ],
          project: {
            title: 'Accessible Venue Feed, Two Query Styles',
            description: 'Build an accessible-venue feed twice against a venue/access-feature/inspection schema: once with relational queries and once with explicit joins plus reshape. Inspect SQL and plans, choose one implementation with evidence, then replace offset pagination with a stable `(verifiedAt, id)` cursor.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day78',
      title: 'Day 78: Migrations, Seeding & Transactions',
      duration: '4 hours',
      topics: [
        {
          id: 'two-workflows',
          title: 'push vs generate + migrate — two workflows, one choice',
          duration: '40 mins',
          items: [
            'drizzle-kit push diffs your schema against the live database and applies the change immediately: no files, no history, perfect for a solo prototype',
            'drizzle-kit generate writes a versioned SQL migration; drizzle-kit migrate applies the pending ones. That is what a team and a production database need',
            'What lands in ./drizzle: numbered .sql files, a snapshot.json per migration (how Drizzle knows the previous state), and meta/_journal.json (the order they apply in). Commit all of it',
            'The rule: push against your own dev database, never against staging or production — push can silently choose a destructive path',
            'Switching from push to generate on an existing database: take one baseline snapshot first, or your first generated migration will try to create tables that already exist',
          ],
        },
        {
          id: 'reading-migrations',
          title: 'Reading and reviewing what was generated',
          duration: '40 mins',
          items: [
            'Open the generated .sql before you run it — it is plain SQL you can read faster than any diff tool can summarise',
            'Renames are ambiguous by definition: drizzle-kit asks whether you renamed a column or dropped one and created another. Answering wrong is how a column of production data disappears',
            'Review migrations in the pull request like code, because that is the only moment the destructive statement is cheap to catch',
            'drizzle-kit check finds conflicting migrations after a branch merge — two teammates who generated from the same snapshot',
            'When the diff cannot express what you want (a backfill, a data fix, a concurrent index), write a custom migration file by hand and let it apply in order with the rest',
          ],
        },
        {
          id: 'applying',
          title: 'Applying migrations in a real deployment',
          duration: '40 mins',
          items: [
            'Drizzle records applied migrations in a table inside your database, so migrate is safe to re-run and applies only what is pending',
            'Option one: drizzle-kit migrate as a release step in CI, before the new code starts serving traffic — the default, and the easiest to reason about',
            'Option two: migrate(db, { migrationsFolder: "./drizzle" }) at boot, for platforms with no release hook. Convenient, but every instance races to run it',
            'Whichever you pick, exactly one process should migrate. Two instances applying the same migration at once is a genuine production incident',
            'Migrations connect directly, not through the pooler — a transaction-mode pooler will reject the statements a migration needs',
            'Have a rollback answer before you deploy: a forward-fix migration is almost always safer than reversing one, but you need to have decided in advance',
          ],
        },
        {
          id: 'safe-evolution',
          title: 'Changing a schema without downtime',
          duration: '35 mins',
          items: [
            'Expand → migrate → contract, in three deploys: add the new nullable column, backfill and dual-write, then drop the old one once nothing reads it',
            'A rename is the same dance — add, backfill, switch reads, drop — because old and new code run simultaneously during any rolling deploy',
            'Adding a NOT NULL column with no default rewrites the table and takes a lock: add it nullable, backfill in batches, then tighten the constraint',
            'Create indexes concurrently on a large live table (a custom migration, since it cannot run inside a transaction)',
            'The question to ask before every migration: what does the currently running version of my code do while this is halfway applied?',
          ],
        },
        {
          id: 'seeding',
          title: 'Seed data you can run twice',
          duration: '30 mins',
          items: [
            'A seed script is just your db client in a file: npm run db:seed → tsx src/db/seed.ts',
            'Make it idempotent with .onConflictDoNothing() so running it twice does not duplicate or explode',
            'Keep three data sets separate: demo data for showing the app, fixtures for tests, and reference data (countries, plans) that belongs in a migration because production needs it too',
            'drizzle-seed generates realistic bulk rows when you need volume to test pagination and index choices honestly',
            'Seed the test database from the same script your CI runs, so a passing local suite means something',
          ],
        },
        {
          id: 'transactions',
          title: 'Transactions and engine-enforced integrity',
          duration: '55 mins',
          items: [
            'await db.transaction(async (tx) => { ... }) — commits when the callback resolves, rolls back if it throws',
            'Every statement inside must use tx, not db. A stray db call escapes the transaction and commits on its own — the single most common Drizzle transaction bug, and it will not error',
            'tx.rollback() aborts deliberately when business logic says no, without inventing a throwaway error',
            'Nested transactions become savepoints, so an inner failure can be recovered from without losing the outer work',
            'Options when you need them: { isolationLevel: "serializable" } for invariants across rows, accessMode: "read only" for reporting queries. Read committed is the default you already know',
            'The integrity you hand-rolled in Mongo on Day 72 is now the engine\'s job: foreign keys, unique constraints, and checks cannot be bypassed by any code path',
            'Concurrency in practice: .onConflictDoUpdate() for a safe upsert, and .for("update") to lock rows you are about to decrement — the stock-and-orders problem, solved by the database',
            'Map errors at your boundary: a unique violation arrives as Postgres code 23505 — turn it into a 409 in the Phase 5 error middleware instead of leaking a 500',
          ],
          project: {
            title: 'Seed-Exchange Ledger: Migrated and Transactional',
            description: 'Create versioned migrations for seed varieties, members, offers, and exchanges; add a column through expand/backfill/contract and write an idempotent seed. Complete an exchange plus inventory decrement transactionally, prove rollback by throwing halfway, and map a duplicate offer key to 409.',
            type: 'mini',
          },
        },
      ],
    },
    {
      id: 'day79',
      title: 'Day 79: Project — Mutual-Aid Inventory Lineage API',
      duration: '5 hours',
      topics: [
        {
          id: 'spec-schema',
          title: 'Spec + schema',
          duration: '60 mins',
          items: [
            'Domain: claims, sources, tags, and confidence notes — every claim traceable to the sources that support it',
            'Model it relationally: claims, sources, claimsToSources (composite PK, plus a stance or weight column on the link), tags, claimsToTags, notes',
            'Enum for confidence level, check constraints for anything the type system cannot enforce, cascade rules you can defend out loud',
            'Indexes chosen from your actual query list, not from the entity list — write the three queries the API must serve first',
            'Generate the baseline migration and run it; seed enough rows that pagination and search are honest',
          ],
        },
        {
          id: 'build-endpoints',
          title: 'Build the API',
          duration: '90 mins',
          items: [
            'CRUD for claims and sources, with Zod schemas derived from the tables (drizzle-zod) rather than hand-written',
            'A relation-heavy read: a claim with its sources, tags, and note count in one db.query call — no N+1',
            'Full-text search over claim text using a Postgres tsvector column and a GIN index, exposed as a filter',
            'Faceted filters (tag, confidence, date range) composed from an optional-condition array, plus keyset pagination',
            'A repository layer between routes and Drizzle, so the routes never import a table directly',
            'Reuse your Phase 5 validation and error middleware unchanged — the data layer changed, the HTTP layer did not',
          ],
        },
        {
          id: 'transaction-integrity',
          title: 'The write that needs ACID',
          duration: '45 mins',
          items: [
            'Ingesting a claim with its sources and tags is one transaction: partial ingestion is corrupt data, not a partial success',
            'Upsert sources by URL with onConflictDoUpdate so re-ingesting the same source does not duplicate it',
            'Deliberate rollback when a referenced source fails validation mid-ingest',
            'Prove it: a test that throws halfway and asserts nothing was written',
          ],
        },
        {
          id: 'test',
          title: 'Test against a real database',
          duration: '55 mins',
          items: [
            'Vitest + Supertest against a real throwaway Postgres, not a mock — the constraints are half of what you are testing',
            'A separate test database (or a Docker container) with migrations applied in global setup',
            'Reset between tests by truncating tables, and keep each test\'s data its own so the suite can run in any order',
            'Cover the paths that matter: the transactional ingest, a constraint violation returning 409, the relation-heavy read, and pagination past the first page',
          ],
        },
        {
          id: 'deploy-decide',
          title: 'Deploy + the decision you can now defend',
          duration: '50 mins',
          items: [
            'Deploy to Render or Railway with DATABASE_URL pointing at the pooled connection string',
            'Run drizzle-kit migrate as a release step against the direct URL, then smoke test the deployed API',
            'Pick relational for strong integrity, complex joins and reporting, and transactional money or inventory',
            'Pick document for flexible or evolving shapes, embedded hierarchies, and fast early iteration',
            'Polyglot persistence is normal: Postgres for the core, something else for the rest — and a repository layer keeps your routes ignorant of which is underneath',
            'Say the Drizzle trade-off in one sentence: schema and types in TypeScript, SQL you can still read and tune, migrations you review like code',
          ],
          project: {
            title: 'Mutual-Aid Inventory Lineage API — DEPLOYED',
            description: 'Build a Postgres/Drizzle API that traces donated goods from intake batch through storage, reservation, handoff, and adjustment. Support full-text search, faceted filters, and cursor pagination; make every multi-record handoff transactional, enforce invariants in the database, seed realistic batch histories, test against a real database, and defend the relational model.',
            type: 'capstone',
            features: [
              'Claims link to sources and tags through explicit join tables; every response preserves provenance.',
              'Search, faceted filters, keyset pagination, deliberate indexes, and an EXPLAIN ANALYZE plan you can explain.',
              'Versioned Drizzle migrations, an idempotent seed script, and integration tests against a real Postgres.',
              'One transactional ingest that is provably all-or-nothing, and constraint violations mapped to correct HTTP status codes.',
            ],
            hints: [
              'Write the three queries the API must serve before you finish the schema — indexes follow queries, not entities.',
              'Upsert sources by URL so re-ingesting the same source is safe to run twice.',
              'Keep raw SQL to the one place it earns its keep (the search query), and let the builder handle the rest.',
            ],
          },
        },
      ],
    },
  ],
  checkpoint: {
    skills: [
      'Mongo + Mongoose: schema, queries, indexes, aggregation, transactions',
      'Population & N+1 avoidance (in Mongoose and any ORM)',
      'Reading explain() output',
      'Put Postgres behind a Node API with Drizzle: schema as TypeScript, constraints, indexes, inferred types',
      'Query with both the builder and relational queries; read the SQL Drizzle compiles and tune it',
      'Versioned migrations, safe zero-downtime schema changes, seeding, and ACID transactions',
      'Choose document vs relational deliberately (and say where a typed query builder beats a raw driver)',
    ],
    milestone: 'Your APIs persist real data in both a document and a relational store. Now make them know who\'s asking.',
  },
};
