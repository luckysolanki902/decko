import RevisionDashboard from '@/components/revision/RevisionDashboard';

export const dynamic = 'force-dynamic';

export default function DamlRevisionDashboardPage() {
  return <RevisionDashboard roadmapId="daml" />;
}
