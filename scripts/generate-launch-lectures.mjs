import fs from 'node:fs';
import path from 'node:path';

const courses = {
  webd: [
    ['A Real Page on the Internet', 'html', 'A page can look fine and still have no meaningful structure.', 'The browser reads HTML as a nested tree, not as a picture.', ['document structure and nesting', 'semantic landmarks', 'links and paths', 'images and alternative text'], `<main>\n  <article>\n    <h1>Saturday repair cafe</h1>\n    <p>Bring one broken household item.</p>\n    <a href="signup.html">Reserve a place</a>\n  </article>\n</main>`, 'Neighbourhood Field Guide'],
    ['Make the Page Readable', 'css', 'Random style tweaks solve one screenshot and create three new inconsistencies.', 'CSS is a decision system: selectors choose elements, the cascade resolves competing declarations, and the browser paints boxes.', ['selectors and declarations', 'inheritance and cascade', 'typography and spacing', 'the box model'], `:root { --space: 1rem; --ink: #1f2933; }\nbody { color: var(--ink); line-height: 1.6; }\narticle { max-width: 65ch; margin: 0 auto; padding: calc(var(--space) * 2); }`, 'Reading Mode Redesign'],
    ['Layouts That Survive Real Content', 'css', 'A card row that fits three short titles can collapse when one title wraps.', 'Layout is negotiation over available space. Normal flow gives content room, Flexbox distributes one axis, and Grid coordinates rows and columns.', ['normal flow', 'Flexbox axes', 'Grid tracks', 'intrinsic sizing and overflow'], `.board { display: grid; grid-template-columns: repeat(auto-fit, minmax(min(16rem, 100%), 1fr)); gap: 1rem; }\n.card { min-width: 0; overflow-wrap: anywhere; }`, 'Community Noticeboard'],
  ],
  daml: [
    ['Your First Investigation', 'python', 'A polished average can be completely wrong when duplicate rows and impossible values are hiding underneath it.', 'EDA is a sequence of questions: what does one row mean, what can be trusted, what patterns exist, and what decision could change?', ['grain and units', 'data-quality checks', 'distribution and segments', 'claims and alternative explanations'], `import pandas as pd\n\nstalls = pd.read_csv("street_food_prices.csv")\nprint(stalls.shape)\nprint(stalls.dtypes)\nprint(stalls.isna().sum())\nprint(stalls.duplicated().sum())`, 'Street-Food Price Mystery'],
    ['Python as an Analysis Tool', 'python', 'Copying the same manual check into twelve spreadsheet tabs makes the result impossible to update safely.', 'A program stores values, makes decisions, repeats work, and packages a reliable transformation behind a name.', ['values and types', 'conditions', 'loops over records', 'functions and return values'], `def valid_duration(seconds):\n    if seconds <= 0:\n        return False\n    return seconds <= 60 * 60\n\ndurations = [182, -3, 241, 7200]\nprint([value for value in durations if valid_duration(value)])`, 'Personal Music Library Audit'],
    ['Pandas Without Magic', 'python', 'A chain of table operations can silently remove most rows if you never predict the shape between steps.', 'A DataFrame is a labelled rectangular value. Every operation should be understood as selecting, transforming, grouping, or combining that rectangle.', ['DataFrame shape and dtypes', 'selection and filtering', 'missing-value treatment', 'group summaries'], `import pandas as pd\n\ngames = pd.read_csv("board_games.csv")\nplayable = games.loc[games["players"].between(games["min_players"], games["max_players"])].copy()\nsummary = playable.groupby("category", as_index=False)["rating"].median()\nprint(summary.sort_values("rating", ascending=False))`, 'Board-Game Night Explorer'],
  ],
  go: [
    ['Your First Useful Binary', 'go', 'A script that only works inside its editor has not yet become a tool.', 'Go turns one main package into a native binary. The compiler checks the whole program before that binary can run.', ['module and package', 'imports and main', 'arguments and output', 'build and exit status'], `package main\n\nimport (\n    "fmt"\n    "os"\n    "strings"\n)\n\nfunc main() {\n    note := strings.Join(os.Args[1:], " ")\n    fmt.Println("focus:", note)\n}`, 'Focus Note CLI'],
    ['Values Have Types and Zero States', 'go', 'Treating every number as interchangeable causes quiet truncation and overflow.', 'A Go variable is a named slot with one fixed type. Its zero value is valid immediately, but each operation still follows that type.', ['declaration and assignment', 'numeric types and conversion', 'zero values', 'overflow and integer division'], `package main\n\nimport "fmt"\n\nfunc main() {\n    used, limit := 7, 10\n    percent := float64(used) / float64(limit) * 100\n    fmt.Printf("%.0f%% used\\n", percent)\n}`, 'Unit Budget Calculator'],
    ['Control Flow Should Read Like Policy', 'go', 'A loop can compile perfectly and still skip the final item or never stop.', 'Control flow is a visible policy for choosing and repeating work. A trace should make every branch and next value predictable.', ['if and scope', 'switch without fallthrough', 'for forms', 'termination and boundaries'], `package main\n\nimport "fmt"\n\nfunc main() {\n    people := 7\n    groups := 3\n    for i := 0; i < people; i++ {\n        fmt.Printf("person %d -> group %d\\n", i+1, i%groups+1)\n    }\n}`, 'Fair Rotation Scheduler'],
  ],
  reactnative: [
    ['Your First Native Interaction', 'tsx', 'A static welcome screen proves the toolchain works, but it does not yet prove you understand how an app responds.', 'React Native asks JavaScript components to describe native views. State changes cause a new description, and the native host updates the screen.', ['Expo development loop', 'native core components', 'props and state', 'press events and rerender'], `import { useState } from 'react';\nimport { Pressable, Text, View } from 'react-native';\n\nexport default function App() {\n  const [count, setCount] = useState(0);\n  return <View><Text>Breaths: {count}</Text><Pressable onPress={() => setCount(value => value + 1)}><Text>Count one</Text></Pressable></View>;\n}`, 'One-Tap Grounding App'],
    ['Layout Without Browser Assumptions', 'tsx', 'A screen that looks balanced on one phone can hide content behind a notch or keyboard on another.', 'React Native layout uses Yoga. Space is negotiated inside native safe areas, with vertical layout as the default.', ['Yoga and Flexbox defaults', 'safe areas', 'text measurement and dynamic type', 'ScrollView versus FlatList'], `import { SafeAreaView, StyleSheet, Text, View } from 'react-native';\n\nexport default function App() {\n  return <SafeAreaView style={styles.screen}><View style={styles.card}><Text style={styles.title}>Today</Text></View></SafeAreaView>;\n}\nconst styles = StyleSheet.create({ screen: { flex: 1, padding: 16 }, card: { flex: 1 }, title: { fontSize: 24 } });`, 'Pocket Checklist'],
    ['Components Carry Product Meaning', 'tsx', 'Copying the same card three times makes every fix a three-place edit and lets states drift apart.', 'A component gives one product idea a name and a contract. Props supply variation; composition keeps the primitive small.', ['composition and props', 'children and variants', 'state ownership', 'tokens and stable list keys'], `type NoteCardProps = { title: string; selected?: boolean };\nfunction NoteCard({ title, selected = false }: NoteCardProps) {\n  return <View accessibilityState={{ selected }}><Text>{title}</Text></View>;\n}`, 'Field Note Card System'],
  ],
};

const lessonNotes = {
  'A Real Page on the Internet': [
    'A document has one outer html element, then a head for information about the page and a body for what people can see. Nesting records containment. If a paragraph sits inside an article in the markup, the browser places that paragraph node inside the article node in its document tree. A missing closing tag can therefore change more than appearance: it can change which content belongs together.',
    'Semantic elements name the job of a region. A main element identifies the unique primary content; nav contains a major navigation group; article marks content that can stand on its own. They do not automatically make a page accessible, but they give browsers and assistive technology a stronger structure than a stack of anonymous div elements.',
    'A link is an address relationship. A relative path starts from the current file, while an absolute web URL names the full location. The beginner trap is testing only from one folder and assuming a path is universally correct. Move the page one directory deeper and predict which links survive before opening it.',
    'Alternative text replaces the information an image contributes when the image cannot be seen. It is not a filename or a caption copied mechanically. A decorative flourish can use empty alternative text; a repair diagram needs words that preserve the instruction. The test is whether the surrounding task still makes sense without the pixels.',
  ],
  'Make the Page Readable': [
    'A CSS rule has a selector that chooses elements and declarations that propose property values. Start with one paragraph and one rule, then inspect which nodes match. A selector that is broader than intended is not merely untidy: it creates invisible coupling, because a future element can inherit a style decision made for something else.',
    'When declarations compete, the browser resolves origin, importance, selector specificity, and source order. Inheritance is separate: some computed values, such as color, pass from parent to child when the child has no winning value. Debug by asking which declaration won and why, not by adding another stronger selector.',
    'Readable typography comes from relationships: line length, line height, size, contrast, and spacing. A 65-character measure keeps the eye from travelling too far; vertical spacing reveals groups. The failure case is a page that looks spacious on a wide monitor but becomes a wall of text on a narrow one.',
    'Every visible element participates in a box made from content, padding, border, and margin. With content-box sizing, declared width covers only content; padding and borders make the rendered box wider. Measure a 200-pixel box with 20-pixel padding and a 2-pixel border before changing box-sizing so the rule is earned rather than memorised.',
  ],
  'Layouts That Survive Real Content': [
    'Normal flow is the browser’s safe default: block boxes stack and grow to fit their content. Removing an element from that flow can make later content overlap it. Before reaching for position, build the document in normal flow and identify the specific relationship flow cannot express.',
    'Flexbox lays items along one main axis and aligns them on the cross axis. Free space is calculated after base sizes and gaps. A common failure is setting flex: 1 on children and expecting long content to disappear; the content still contributes a minimum size unless shrinking and wrapping are allowed.',
    'Grid defines tracks, then places items into the resulting rows and columns. repeat(auto-fit, minmax(...)) is useful because the container, not a media-query guess, decides how many cards fit. Test with one very long title and one narrow viewport to expose intrinsic sizing assumptions.',
    'Overflow is evidence that a child’s minimum contribution is larger than the space offered. min-width: 0 lets a grid or flex child shrink below its content-based minimum, while overflow-wrap controls text breaking. Hiding overflow may conceal the symptom without fixing the layout contract.',
  ],
  'Your First Investigation': [
    'Grain answers “what does one row represent?” A menu-item table and a purchase-event table can both contain price, but averaging them answers different questions. Write the currency and serving unit beside every price, then check whether duplicate keys mean repeated purchases or accidental duplicate rows.',
    'Quality checks are claims about allowable data. Price cannot be negative, stall name should not be blank, and one listing key should identify one listing. Count failures before removing anything. Silent cleaning makes the final number impossible to audit because nobody can see what evidence was discarded.',
    'A mean compresses a distribution into one number and can hide a long tail. Compare count, median, quantiles, and a histogram for each dish or neighbourhood. If one segment has few stalls, its dramatic average may be noise rather than a stable market pattern.',
    'An observation is not yet an explanation. Higher evening prices could reflect location, portion size, demand, missing values, or a collection change. State at least two plausible causes and name the next comparison that would separate them. Analysis becomes useful when it tells us what evidence would change the claim.',
  ],
  'Python as an Analysis Tool': [
    'A value has a type because operations need a meaning. Adding two integers performs arithmetic; adding two strings joins text. The dangerous case is a number read as text: sorting “100” before “20” is legal string behaviour but wrong analytical behaviour. Inspect type before trusting an operation.',
    'A condition turns a definition into a yes-or-no decision. Boundaries belong in the definition: if a valid track lasts from 1 through 3600 seconds, test 0, 1, 3600, and 3601 explicitly. A loop then applies that same decision to each record without changing the rule halfway through.',
    'A loop should have a traceable state: current record, decision, and any accumulated result. Print those three for a tiny list before processing a file. Off-by-one errors and accidental reuse of an old value become visible when the trace is small enough to simulate by hand.',
    'A function gives a transformation a name and returns a result to its caller. Printing inside a function is not the same as returning: printed text is for a person, while a return value can be tested and reused. Call the function with boundary values and compare actual results with the written contract.',
  ],
  'Pandas Without Magic': [
    'A DataFrame is a labelled two-dimensional value. Shape reports rows and columns; dtypes report how each column is represented. An object dtype in a numeric field is a warning that at least one value may be text. Inspect both before calculation so conversion errors do not arrive deep in the analysis.',
    'Selection chooses labels; filtering keeps rows whose Boolean mask is true. Write the expected row count before applying a filter, then compare it with the result. A missing pair of parentheses can change operator grouping, while chained assignment can modify a temporary object rather than the table you meant to change.',
    'Missing does not mean zero. It may mean not collected, not applicable, or failed to parse, and each meaning suggests a different treatment. Measure missingness by column and segment before filling or dropping. A single global fill can erase the very pattern the investigation needs to explain.',
    'Groupby splits rows by key, applies an aggregation, and combines one result per output group. The output grain changes. Count rows and unique groups first, then manually calculate one group so the method is not magic. A mean without its count can make a tiny group look as trustworthy as a large one.',
  ],
  'Your First Useful Binary': [
    'A Go module names a versioned unit of code; a package groups files compiled together. The main package is special because it can produce an executable. Run go mod init once, then notice that go run compiles to a temporary program while go build leaves a reusable binary.',
    'Imports declare package dependencies and main is the process entry point. Go rejects unused imports because dead dependencies often signal unfinished reasoning. Start with fmt, compile, then add one dependency only when the next behaviour needs it.',
    'os.Args contains the executable name followed by command-line arguments. Joining every argument works for a happy path but an empty slice produces an empty note. Decide whether empty input is valid; if not, write the error to stderr and choose a non-zero exit status so scripts can detect failure.',
    'A binary communicates through output, error output, and its exit status. Human-readable text is not enough for composition. Run the program from another directory and inside a shell condition to prove that it is a tool rather than an editor-bound demo.',
  ],
  'Values Have Types and Zero States': [
    'Short declaration creates variables and infers their types from initial values; assignment changes existing variables. The distinction matters inside nested scopes, where := can create a new variable that shadows the one you intended to update. Print both scopes in a tiny example to see the two storage locations.',
    'Integer division discards the fractional remainder before any later conversion. Converting 7/10 to float produces 0, while converting each operand first produces 0.7. Place the conversion where meaning changes, and reject incompatible units instead of casting until the compiler becomes quiet.',
    'Every Go type has a zero value, so declared variables begin in a valid language state: 0, false, empty string, or nil. Valid does not mean meaningful for the domain. A zero budget limit may need explicit rejection even though the program can represent it safely.',
    'Fixed-width integers have finite ranges, and arithmetic can wrap. Choose a type from the domain and expected range rather than habit. For a budget calculator, also guard a zero limit before division and test a value near the chosen type’s boundary.',
  ],
  'Control Flow Should Read Like Policy': [
    'An if statement should make the domain decision visible. Initializers keep a temporary value near the condition but limit its scope to the branches. Use that deliberately; do not redeclare a result inside the block and then wonder why the outer value stayed unchanged.',
    'Go switch cases do not fall through by default. That makes a switch suitable for mutually exclusive policy states. Include a default when unknown input needs a deliberate response, and test one value from every case rather than trusting visual coverage.',
    'Go has one loop keyword, for, with several forms. The three-part form exposes initialization, continuation, and update. Trace i before the body and after the update for people counts 0, 1, and 7. Those cases reveal skipped work and incorrect boundaries quickly.',
    'Termination is an invariant: each iteration must move some state toward the stop condition. In a fair rotation, modulo maps an increasing person index into a bounded group range. Prove that the result always stays between zero and groups minus one before formatting it for people.',
  ],
  'Your First Native Interaction': [
    'Expo provides the development loop, but the app still runs as native views on a device or simulator. Change one Text value and observe fast refresh, then restart the app to separate source-code updates from state that only lived in memory.',
    'View groups content, Text renders text, and Pressable exposes press interaction. These are not HTML tags and browser defaults do not apply. Nest visible strings inside Text and use accessibility roles and labels where the visual content does not fully name the action.',
    'Props are inputs from a parent; state is memory owned by a component instance. Calling the state setter schedules another render with a new value. Log the render and press handler separately to see the event cause a new description rather than directly editing a screen node.',
    'A functional state update receives the latest queued value. setCount(value => value + 1) therefore survives multiple queued presses better than reading a captured count repeatedly. Test several rapid presses and include a reset so both forward and boundary transitions are visible.',
  ],
  'Layout Without Browser Assumptions': [
    'Yoga calculates React Native layout using Flexbox-like rules, but the default main axis is vertical. A View with flex: 1 asks for remaining space from its parent; it does not mean “one unit” without a sized parent. Sketch the parent’s available rectangle before adjusting children.',
    'Safe areas represent screen regions obstructed by hardware or system UI. Padding with a guessed status-bar height works on one device and fails on another. Put screen content inside the platform-safe boundary, then test portrait, landscape, and a device with a cutout.',
    'Text size is content, not decoration. Dynamic type can make a one-line label wrap or grow beyond a fixed-height card. Remove fixed heights, allow wrapping, and test the largest accessibility text setting before declaring the layout complete.',
    'ScrollView renders all children and suits a small bounded document. FlatList renders a window and suits long repeated data. Choosing ScrollView for hundreds of rows can make startup and memory worse even though the first screenshot looks correct.',
  ],
  'Components Carry Product Meaning': [
    'A component should name a product idea such as NoteCard, not merely hide a pile of styles. Its props form a contract: required title, optional selected state, and events the parent may handle. If every caller needs escape-hatch props, the abstraction probably owns the wrong boundary.',
    'Composition lets a small component accept content without predicting every future variation. A variant should represent a real semantic state, not a one-off colour request. Keep visual tokens shared so selected, pressed, and disabled states remain consistent across uses.',
    'State belongs at the nearest common owner that needs to coordinate it. If each card stores its own selected flag, the list cannot reliably enforce single selection. Lift the selected identifier to the list and pass each card a derived Boolean plus an event callback.',
    'List keys identify item continuity across renders. An array index changes meaning when items move, so state can appear on the wrong row. Use a stable data identifier, then insert an item at the front to verify that selection and component identity stay attached to the same note.',
  ],
};

const labels = ['the mental model', 'the first mechanism', 'the failure boundary', 'the studio decision'];
const correctIds = ['b', 'c', 'a', 'd', 'b', 'a', 'c', 'd', 'a', 'c'];

function midQuiz(concept, index) {
  const options = [
    'Recognise the term in a finished example.',
    'Predict a result, explain the mechanism, and test a boundary.',
    'Copy the same surface steps with new names.',
    'Add features before checking the underlying assumption.',
  ];
  const correctPosition = [1, 2, 0][index % 3];
  const ordered = options.map((text, optionIndex) => ({ text: options[(optionIndex - correctPosition + 1 + options.length) % options.length], correct: optionIndex === correctPosition }));
  return `\n\n\`\`\`quiz\n${JSON.stringify({ prompt: `What would provide the strongest evidence that you understand ${concept}?`, multiple: false, options: ordered, explanation: `Understanding ${concept} means producing a prediction and explanation that survive a changed input, not recognising familiar wording.` }, null, 2)}\n\`\`\`\n`;
}

function finalQuiz(title, concepts) {
  const bank = [
    [`What is the strongest evidence that ${concepts[0]} is understood?`, `Predict and explain ${concepts[0]}, then test a boundary.`, 'Repeat the definition word for word.', 'Recognise it in the reference example.', 'Use it once without checking the result.'],
    ['Why should the first plausible attempt be allowed to fail?', 'The failure reveals the assumption that the next tool must repair.', 'Failure makes the final artifact look more advanced.', 'Every first attempt is necessarily wrong.', 'Errors remove the need to explain the mechanism.'],
    ['What should happen before running a changed input?', 'Write the expected result and the reason for it.', 'Add another abstraction.', 'Read the completed answer again.', 'Remove every error state.'],
    ['What makes a mental model useful?', 'It predicts behaviour outside the exact example.', 'It uses the most technical vocabulary.', 'It replaces testing.', 'It describes only the normal case.'],
    ['Which observation deserves investigation?', 'Actual behaviour differs from a written prediction.', 'The copied example produces its copied output.', 'The syntax highlighting changes colour.', 'The file becomes longer.'],
    ['What makes the studio artifact evidence of learning?', 'It works under a changed requirement and its mechanism can be explained.', 'It contains the largest possible feature list.', 'It looks identical to the reference.', 'It avoids all boundary cases.'],
    ['Which task best tests transfer?', 'Solve a changed problem without being told which technique to select.', 'Retype the same solution from memory.', 'Reread the explanation immediately.', 'Rename variables in the reference.'],
    ['Why compare expected and actual results?', 'The difference locates a gap in the current explanation.', 'Matching results prove every future case.', 'It removes the need for boundary tests.', 'It guarantees the code has no defects.'],
    ['What should happen after receiving help?', 'Close the help and reconstruct the critical step independently.', 'Keep the answer open during every later attempt.', 'Skip the changed task.', 'Count the assisted result as independent mastery.'],
    [`Which statement best summarizes ${title}?`, 'Build a useful result, explain why it works, and test where it breaks.', 'Memorise the final syntax and avoid changing it.', 'Treat a polished normal case as proof of mastery.', 'Prefer more features to a clearer mechanism.'],
  ];
  const questions = bank.map(([prompt, correct, ...distractors], index) => {
    const correctIndex = ['b', 'c', 'a', 'd', 'b', 'a', 'c', 'd', 'a', 'c'].indexOf(correctIds[index]);
    const texts = [...distractors];
    texts.splice(correctIndex, 0, correct);
    const options = texts.map((text, optionIndex) => ({ id: ['a', 'b', 'c', 'd'][optionIndex], text }));
    return { id: `q${index + 1}`, type: 'single_correct', prompt, codeSnippet: null, options, correctOptionIds: [correctIds[index]], explanation: `The correct response requires evidence about ${labels[index % labels.length]}, not familiarity with the example.`, example: `Change one input or constraint and predict the result before running it.` };
  });
  return `\n\n\`\`\`finalquiz\n${JSON.stringify({ title: `Day quiz: ${title}`, questions }, null, 2)}\n\`\`\`\n`;
}

function lecture(course, number, spec) {
  const [title, language, failure, model, concepts, example, build] = spec;
  const sections = concepts.map((concept, index) => `\n---\n\n## ${index + 1}. ${concept[0].toUpperCase()}${concept.slice(1)}\n\nBefore reading on, write one prediction about a boundary case. Keep it visible so the result can correct your model instead of merely surprising you.\n\n${lessonNotes[title][index]}\n\nUse this trace while you work:\n\n\`\`\`text\nstarting state -> operation -> expected state\nstarting state -> boundary case -> expected result\nobserved result -> difference -> next check\n\`\`\`\n\nDo not move on after the normal case. Change one condition and explain why the mechanism predicts the new result.${index < 3 ? midQuiz(concept, index) : ''}` ).join('');
  return `# Day ${number}: ${title}\n\n**Duration: 4 hours | Focus: build a useful artifact, explain the mechanism, and verify a boundary**\n\n## Why this day exists\n\nYou can get a convincing result without understanding why it works. That is dangerous because the first realistic change then feels like a completely new problem.\n\nToday ends with **${build}**, but the artifact is not the only goal. You will predict behaviour, compare it with evidence, and explain the first failure instead of hiding it.\n\n---\n\n## The version that looks finished\n\n${failure}\n\nThe first attempt is not foolish. It is the shortest path to a visible result, and that early win matters. The problem appears when we ask the result to survive a changed input, a second user, a restart, or a stricter definition of correctness.\n\nWrite down what you expect the first attempt to do in the normal case and in one boundary case. Keep the prediction visible.\n\n---\n\n## The mental model\n\n> ${model}\n\n\`\`\`text\ninput -> representation -> operation -> visible result\n              |              |\n           assumptions    failure evidence\n\`\`\`\n\nA useful mental model predicts. If it only restates vocabulary, it is not doing enough work.${sections}\n\n---\n\n## Put the pieces together\n\nHere is a small reference point. Read it once, then cover it and label what each line contributes to the model.\n\n\`\`\`${language}\n${example}\n\`\`\`\n\nDo not add features yet. First change one input and predict the output. Then break one assumption deliberately. The resulting error, wrong value, or awkward interaction is evidence about the boundary of the current design.\n\n---\n\n## Practice\n\n1. Reconstruct the smallest working version without looking.\n2. Add one boundary case and write the expected result before running it.\n3. Explain the mechanism without using the day title as a definition.\n4. Change the task so the copied version no longer fits.\n5. Record whether you completed each step with help, independently, or after correction.\n\n---\n\n## Studio: ${build}\n\nBuild the smallest version another person could actually use. Include one empty or error state, one constraint that forces a decision, and one check comparing expected with actual behaviour.\n\nWhen it works, change one requirement. The second version is the transfer test. It should require the same underlying ideas without repeating the same surface steps.\n\n---\n\n## Common mistakes\n\n| Mistake | Why it hurts | Better move |\n|---|---|---|\n| Treating the first success as mastery | The normal case hides weak assumptions | Test a boundary immediately |\n| Adding features before tracing state | More code creates more places to guess | Write the state transition first |\n| Reading the answer after a miss | Familiarity returns without reconstruction | Close it and rebuild the critical step |\n| Testing only the copied task | Memory of syntax can imitate understanding | Change the data, constraint, or user need |\n\n---\n\n## Cheat sheet\n\n\`\`\`text\n1. Name the problem.\n2. Predict before running.\n3. Trace the representation and operation.\n4. Break one assumption deliberately.\n5. Rebuild the critical path without looking.\n6. Transfer the idea to a changed task.\n\`\`\`${finalQuiz(title, concepts)}`;
}

for (const [course, specs] of Object.entries(courses)) {
  const directory = path.join(process.cwd(), 'public', 'data', 'lectures', course, 'phase1');
  fs.mkdirSync(directory, { recursive: true });
  specs.forEach((spec, index) => {
    const slug = spec[0].toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
    fs.writeFileSync(path.join(directory, `day0${index + 1}-${slug}.md`), lecture(course, index + 1, spec));
  });
}
